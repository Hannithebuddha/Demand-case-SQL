# Demand-case-SQL
Анализ продаж и качества прогноза в FMCG с помощью SQL

О проекте

Этот проект — портфельный SQL-кейс на данных условной FMCG-компании FreshBite Foods, производителя продуктов питания.
Основная задача проекта — показать применение SQL к типовым задачам Demand Planning и анализа продаж: проверке качества данных, анализу объёмов, промо-зависимости и качества прогноза.
Проект сознательно сфокусирован на объёмах продаж, а не на выручке или марже, поскольку основной бизнес-контекст кейса связан с Demand Planning.
Бизнес-задача

Компания наблюдает неоднородную динамику продаж и ухудшение качества прогноза.
Необходимо:
проверить качество исходных данных;
проанализировать динамику объёмов;
определить различия между категориями и каналами;
оценить влияние промо;
сравнить Current forecast с фактическими продажами;
рассчитать forecast error, bias и WAPE;
определить основные источники ошибки прогноза.

Каналы продаж
Federal chain
Local chain
Discounters
E-commerce

Версии прогноза
Budget
Сurrent
В основном анализе качества прогноза используется версия Current.

Структура данных

products
Справочник продуктов.
Основные поля:
product_id
product_name
category
sub_category
brand
pack_size
launch_date
status
customers

Справочник клиентов.
Основные поля:
customer_id
customer_name
channel
region
customer_type
sales

Фактические продажи.
Гранулярность таблицы:
одна строка = месяц × продукт × клиент
Основные поля:
sale_date
product_id
customer_id
quantity
promo_flag
forecast

Прогноз продаж.
Гранулярность таблицы:
одна строка = месяц × продукт × клиент × версия прогноза
Основные поля:
forecast_month
product_id
customer_id
forecast_quantity
forecast_version
calendar

Календарный справочник.
Основные поля:
date
year
month
month_name
quarter

Логика, заложенная в тестовые данные
В синтетическом датасете специально заложены несколько FMCG-сценариев:
категория Dairy растёт быстрее бизнеса в целом;
Snacks сильнее зависят от промо;
Beverages имеют выраженную летнюю сезонность;
один крупный клиент постепенно снижает объёмы;
часть SKU систематически overforecast;
часть SKU систематически underforecast;
отдельные продукты запускаются в середине периода;
некоторые продукты имеют статус Discontinued;
присутствует небольшое количество NULL, нулевых и отрицательных объёмов;
специально добавлен дубль на ожидаемой гранулярности;
присутствуют случаи forecast без actual и actual без forecast.

Эти особенности позволяют использовать проект не только для демонстрации SQL-синтаксиса, но и для проверки бизнес-логики данных.

SQL-навыки, которые демонстрирует проект:
SELECT
WHERE
ORDER BY
GROUP BY
HAVING
JOIN
CASE WHEN
агрегатные/агрегирующие функции
COUNT(DISTINCT)
подзапросы
CTE
conditional aggregation
базовые функции работы с датами
работа с NULL
расчёт forecast error
расчёт bias
расчёт WAPE


SQL-блоки проекта

00_schema.sql
Создание структуры таблиц в PostgreSQL.

01_data_quality.sql
Первичная проверка данных:
период данных;
количество строк;
количество уникальных SKU и клиентов;
NULL;
нулевые и отрицательные объёмы;
дубли;
соответствие справочникам;
продажи до даты запуска продукта;
проверка forecast versions.

02_sales_analysis.sql
Анализ фактических объёмов:
динамика по месяцам;
категории;
каналы;
категория × канал;
топ SKU;
годовая динамика;
новые продукты;
discontinued продукты;
динамика клиентов.

03_promo_analysis.sql
Анализ промо-зависимости.

04_forecast_accuracy.sql
Сравнение Current forecast с фактом и расчёт основных KPI качества прогноза.

05_business_insights.sql
Финальные аналитические выборки для формирования выводов и рекомендаций.
